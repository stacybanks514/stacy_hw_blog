<div class="header">
<h1 class="blogtitle">Stacy's Blog</h1>
</div>
<div class="container">
<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th>Body</th>
      <th>Date/Time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if @posts.empty? %>
      <td class="noposts">No Posts available</td>
    <% else %>
    <% @posts.each do |post| %>
      <tr>

        <td class="subject"><%= post.subject %></td>
        <td class="body"><%= post.body %></td>
        <td class="published_at"><%= post.published_at %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td class="notice"><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>
<div class="links">
<%= link_to 'New Post', new_post_path %>
<%= form_tag(search_posts_path, method: "get") do %>
  <%= label_tag(:q, "Search for:") %>
  <%= text_field_tag(:q) %>
  <%= submit_tag("Search") %>
<% end %>
<a href="https://www.google.com/">Search Google</a>
<br>
<%= link_to 'Home', root_path %>
</div>
</div>